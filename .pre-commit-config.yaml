repos:
  - repo: local
    hooks:
      - id: run-simbacore-tests
        name: Run SimbaCore smoke tests (post-commit)
        entry: bash scripts/run-tests.sh
        language: system
        pass_filenames: false
        stages: [post-commit]


